Javascript execution context

{}-> global EC->this


1-global execution context
2- function execution context
3- Eval execution context



how we can execute Javascript
1- memory creation phase(same time it called creation phase)- variable or delacration place alocate
2- execution phase- +,-, % will be excute in this phase


-> following are the example
let val1 = 10;
let val2 = 5;

function addNum(num2, num2){
    let total = num1+num2
    return total
}

let result1 = addNum(val1, val2);
let result2 = addNum(10, 2);

1- gobal execution->this( for run code and alocate in thiskeyword )this is first cycle
2 memory phase -> collect all variable for keep
val1 - undefined
val2 - undefined
addNum - defination(function)
result 1-> undefined
result 2-> undefined

3- excution phase
val1 <- 10
val2 <- 5
addNum -> 1 new variable enviroment+ execution thread(delete after work done)
result1 ->15
result2->
^
|
memorypahse
val1 -> undefined
val2-> undefined
total-> undefined

execaution context
num1 -> 10
num2-> 5
total -> 15(globalexecution in return)


